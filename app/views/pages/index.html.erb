<script type="text/javascript">
  $(function(){
    $(".navbar").removeClass("navbar-devise");
    $(".navbar").addClass("navbar-default");
});
</script>

<div class="jumbotron">
  <%= render 'partial/navbar' %>



 <!-- ワイン名検索 --> 
<!-- searchform -->
<div class="top-search-field">
  <%= form_tag search_path, method: :get do %>
  <div class="row">
    <div class="container">

    <%= hidden_field_tag :lat %>
    <%= hidden_field_tag :lng %>

      <div class="col-md-7 col-xs-10" style="padding-right: 0px">
        <%= text_field_tag :search, params[:search],placeholder:"ワインを探したい地域を入力　➡️　近くの美味しいワインを探す",class: "form-control" ,id:"top-search-field"  %>
    </div>
    <div class="col-md-5 col-xs-2" style="padding-left: 0px;">
        <%= submit_tag "検索", class: "btn btn-danger" %>
    </div>

<!-- ワイン名検索 -->
</div>
</div>   
<% end %>
</div>  
<!-- サーチフォーム -->

</div>


<!-- <div class="container">   -->


 
  <!--おすすめのワイン-->
  <section id="portfolio" class="bg-light-gray row-space-top-3 row-space-3">
    <div class="container">
      <div class="row row-space-2">
        <div class="col-lg-12 text-left">
          <h3 class="section-heading">このサイトの使い方</h3><hr>
          <h4 class="section-subheading">・検索画面で「渋谷」などと入力し近くの美味しいワインを探してみましょう。</h4>
          <h4 class="section-subheading">・美味しいワインについてシェアしましょう。</h4>
        </div>
      </div>

<!--       <div class="row">
        <% @users.each do |user|%>
          <% if !user.listings.blank?%>
            <div class="col-md-4 col-sm-6 portfolio-item row-space-2">
              <%= link_to listing_path(user.listings[0]) do %>
                <%= image_tag user.listings[0].photos[0].image.url(:medium) %>
              <% end %>
              <div class="portfolio-caption">
                <%= image_tag user.image,class:"img-circle profile-1"%>
                <span style="font-weight: bold;"><%= user.name %>さん</span>
              </div>
            </div>
          <% end %>
        <% end %>
      </div> -->
    </div>
  </section>
</div>

<%= render 'pages/footer' %>

<script>
function init() {
  
  var input = /** @type {!HTMLInputElement} */(
      document.getElementById('top-search-field'));

  var autocomplete = new google.maps.places.Autocomplete(input);


  autocomplete.addListener('place_changed', function() {
    var place = autocomplete.getPlace();

    var lat = place.geometry.location.lat();
    var lng = place.geometry.location.lng();

    document.getElementById('lat').value = lat ;
    document.getElementById('lng').value = lng ;


    if (!place.geometry) {
      window.alert("Autocomplete's returned place contains no geometry");
      return;
    }

   
  });
}

</script>


<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC2WAhbQcTWG0At7bXg_TQ1C3jFAH1U1Fo&signed_in=true&libraries=places&callback=init"
    async defer></script>